language: node_js

os: linux
dist: bionic

node_js:
  - 10.15.3
  - lts/*

matrix:
  allow_failures:
    - node_js: lts/*

cache: npm

before_install:
  - npm install -g npm@6.14.5
install:
  - npm install

script:
  - npm test
  - npm run security-checks
